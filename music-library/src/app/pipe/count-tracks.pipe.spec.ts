import { CountTracksPipe } from './count-tracks.pipe';

describe('Pipe: CountTracks', () => {
  it('create an instance', () => {
    const pipe = new CountTracksPipe();
    expect(pipe).toBeTruthy();
  });
});
